(() => {
  const text = {
    en: {
      photoSoon: "Photo coming soon",
      introEyebrow: "Birthday mission",
      coverKicker: "The Midnight",
      coverMain: "Birthday Howl",
      coverName: "for Polina",
      introCopy: "This little page is made to bring you a smile from far away: a bit of moonlight, a pack of wolves, and a warm birthday wish from a good friend.",
      start: "Leeet's Gooooo",
      next: "Next",
      no: "No",
      finalEyebrow: "Final panel",
      finalTitle: "You are celebrated today",
      finalPhoto: "Your picture here",
      finalCopy: "Polina, I hope your birthday feels warm, bright, and full of little moments that remind you how appreciated you are.",
      reasonDefault: "Tap a word to reveal a birthday wish.",
      gallery: "Open memory gallery",
      restart: "Play again",
      galleryTitle: "Polina gallery",
      secretEyebrow: "Hidden panel",
      secretTitle: "A little extra light",
      secretCopy: "Even from far away, this little birthday mission was made with real care. Happy Birthday, Polina.",
      musicMissing: "Music can be added later as assets/birthday-song.mp3.",
      musicPlaying: "Music started.",
      musicPaused: "Music paused.",
      photoMissing: "Add this image later in the assets folder.",
      modalCaption: "Add your image later in the assets folder.",
      secretToast: "Hidden birthday panel unlocked.",
      noTexts: ["No", "Are you sure?", "The pack disagrees", "Try Next", "Birthday law says no"],
      tokens: ["YOU", "WILL", "ALWAYS", "BE", "STRONG"],
      reasons: [
        "May you feel seen, valued, and gently celebrated today.",
        "May this year bring you courage, calm, and many good surprises.",
        "May you always find light, even on quiet days.",
        "May you be surrounded by people who truly appreciate your heart.",
        "May you stay strong, free, and true to yourself."
      ],
      scenes: [
        ["Mission start", "Scene 01", "Ready to run into your birthday?", "Today is officially your moonlit hero day."],
        ["Signal found", "Scene 02", "A birthday signal crossed the snow.", "It travelled with good thoughts, winter sparkle, and one clear mission: make you smile."],
        ["Wolf power", "Scene 03", "Your wolf heart is fully charged.", "Strong, loyal, free, and stubborn enough to follow your own road."],
        ["Memory spark", "Scene 04", "A little memory spark joined the pack.", "Some moments are small, but they still know how to glow when they matter."],
        ["Wolf patrol", "Scene 05", "The wolf squad is on birthday patrol.", "They are guarding the good mood and chasing away boring moments."],
        ["Northern route", "Scene 06", "The northern lights found a path.", "Across distance and noise, straight toward one person who deserves a happy day."],
        ["Smile rescue", "Scene 07", "Operation Smile Rescue is almost complete.", "If this page brings even one tiny smile, the mission was already worth it."],
        ["Final howl", "Scene 08", "One last howl before the big wish?", "A little chaos, a little sparkle, and a lot of birthday energy."],
        ["Surprise delivery", "Scene 09", "The birthday surprise made it.", "It arrived with warmth, a bit of northern drama, and a quiet reminder that you are appreciated."]
      ]
    },
    ru: {
      photoSoon: "Фото будет позже",
      introEyebrow: "Миссия на день рождения",
      coverKicker: "Полуночный",
      coverMain: "волчий день рождения",
      coverName: "для Полины",
      introCopy: "Эта маленькая страница сделана, чтобы передать тебе улыбку издалека: немного лунного света, стая волков и теплое поздравление от хорошего друга.",
      start: "Погнали",
      next: "Дальше",
      no: "Нет",
      finalEyebrow: "Финальная панель",
      finalTitle: "Сегодня празднуют тебя",
      finalPhoto: "Твое фото здесь",
      finalCopy: "Полина, пусть твой день рождения будет теплым, светлым и полным маленьких моментов, которые напоминают тебе, как сильно тебя ценят.",
      reasonDefault: "Нажми на слово, чтобы открыть пожелание.",
      gallery: "Открыть галерею",
      restart: "Сыграть снова",
      galleryTitle: "Галерея Полины",
      secretEyebrow: "Скрытая панель",
      secretTitle: "Еще немного света",
      secretCopy: "Даже издалека эта маленькая праздничная миссия сделана с настоящим теплом. С днем рождения, Полина.",
      musicMissing: "Музыку можно добавить позже как assets/birthday-song.mp3.",
      musicPlaying: "Музыка включилась.",
      musicPaused: "Музыка на паузе.",
      photoMissing: "Добавь это фото позже в папку assets.",
      modalCaption: "Добавь изображение позже в папку assets.",
      secretToast: "Скрытая праздничная панель открыта.",
      noTexts: ["Нет", "Точно?", "Стая против", "Лучше дальше", "Праздник не разрешает"],
      tokens: ["ТЫ", "БУДЕШЬ", "ВСЕГДА", "ОСТАВАТЬСЯ", "СИЛЬНОЙ"],
      reasons: [
        "Пусть сегодня ты чувствуешь, что тебя видят, ценят и по-настоящему берегут.",
        "Пусть этот год принесет тебе смелость, спокойствие и много хороших сюрпризов.",
        "Пусть ты всегда находишь свет, даже в тихие и сложные дни.",
        "Пусть рядом будут люди, которые правда ценят твое сердце.",
        "Пусть ты остаешься сильной, свободной и верной себе."
      ],
      scenes: [
        ["Старт миссии", "Сцена 01", "Готова ворваться в свой день рождения?", "Сегодня официально твой день под лунным светом."],
        ["Сигнал найден", "Сцена 02", "Поздравительный сигнал прошел через снег.", "Он несет хорошие мысли, зимний блеск и одну важную задачу: заставить тебя улыбнуться."],
        ["Сила волка", "Сцена 03", "Твое волчье сердце полностью заряжено.", "Сильное, верное, свободное и достаточно упрямое, чтобы идти своей дорогой."],
        ["Искра памяти", "Сцена 04", "Маленькая искра памяти присоединилась к стае.", "Некоторые моменты маленькие, но они все равно умеют светиться, когда это важно."],
        ["Волчий патруль", "Сцена 05", "Волчья команда вышла на праздничный патруль.", "Они охраняют хорошее настроение и прогоняют скучные моменты."],
        ["Северный путь", "Сцена 06", "Северное сияние нашло дорогу.", "Через расстояние и шум, прямо к человеку, который заслуживает счастливый день."],
        ["Спасение улыбки", "Сцена 07", "Операция Улыбка почти завершена.", "Если эта страница подарит тебе хотя бы маленькую улыбку, миссия уже была не зря."],
        ["Финальный вой", "Сцена 08", "Один последний вой перед главным пожеланием?", "Немного хаоса, немного блеска и много энергии дня рождения."],
        ["Доставка сюрприза", "Сцена 09", "Праздничный сюрприз добрался.", "Он пришел с теплом, северной драмой и тихим напоминанием, что тебя ценят."]
      ]
    }
  };

  const state = { lang: "en", screen: 0, noCount: 0, found: new Set(), toastTimer: null };
  const $ = selector => document.querySelector(selector);
  const screensRoot = $("#screens");
  const progress = $("#progress");
  const song = $("#song");
  const musicButton = $("#musicButton");
  const toast = $("#toast");
  const photoModal = $("#photoModal");
  const modalPreview = $("#modalPreview");
  const modalCaption = $("#modalCaption");
  const galleryModal = $("#galleryModal");
  const galleryGrid = $("#galleryGrid");
  const secretModal = $("#secretModal");

  const t = key => text[state.lang][key];
  const photoPath = key => `assets/photo-${key}.jpg`;

  const template = (strings, ...values) => strings.reduce((out, part, i) => out + part + (values[i] ?? ""), "");

  const screenIntro = () => template`
    <section class="screen intro-screen" data-screen="0">
      <div class="hero-comic" role="img" aria-label="Wolf and moon birthday card">
        <span class="burst-word word-one">HOWL</span>
        <span class="burst-word word-two">MOON</span>
        <span class="burst-word word-three">WISH</span>
        <span class="moon-badge"></span>
        <span class="wolf-mask" aria-hidden="true"></span>
        <div class="photo-placeholder" data-photo-key="01"><span data-key="photoSoon"></span></div>
      </div>
      <p class="eyebrow" data-key="introEyebrow"></p>
      <h1 class="cover-title">
        <span class="cover-kicker" data-key="coverKicker"></span>
        <span class="cover-main" data-key="coverMain"></span>
        <span class="cover-name" data-key="coverName"></span>
      </h1>
      <p class="copy" data-key="introCopy"></p>
      <button class="primary-action" type="button" data-next data-key="start"></button>
    </section>`;

  const screenScene = (scene, index) => {
    const photo = String(index + 2).padStart(2, "0");
    const tilt = ["tilted-left", "tilted-right", "straight-panel", "runner-panel", "battle-panel"][index % 5];
    const side = index % 2 ? "scene-photo-left" : "scene-photo-right";
    const sticker = ["sticker-a", "sticker-b", "sticker-c"][index % 3];
    return template`
      <section class="screen question-screen" data-screen="${index + 1}">
        <div class="panel-card ${tilt}">
          <div class="scene-photo ${side}" data-photo-key="${photo}"><span>Photo ${photo}</span></div>
          <span class="sticker ${sticker}">${scene[0]}</span>
          <p class="panel-label">${scene[1]}</p>
          <h2>${scene[2]}</h2>
        </div>
        <p class="copy">${scene[3]}</p>
        <div class="answer-grid">
          <button class="answer next" type="button" data-next>${t("next")}</button>
          <button class="answer no" type="button" data-no>${t("no")}</button>
        </div>
      </section>`;
  };

  const screenFinal = () => template`
    <section class="screen final-screen" data-screen="10">
      <p class="eyebrow" data-key="finalEyebrow"></p>
      <div class="final-hero-row">
        <h2 data-key="finalTitle"></h2>
        <div class="final-photo" data-photo-key="11"><span data-key="finalPhoto"></span></div>
      </div>
      <p class="copy" data-key="finalCopy"></p>
      <div class="reason-sky" aria-label="Birthday wishes">
        ${t("tokens").map((token, i) => `<button class="reason-token token-${"abcde"[i]}" type="button" data-reason="${i}"><span>${token}</span></button>`).join("")}
      </div>
      <p class="reason-text" id="reasonText" data-key="reasonDefault"></p>
      <div class="memory-strip">
        <button type="button" data-photo-key="02"><span>Photo 02</span></button>
        <button type="button" data-photo-key="03"><span>Photo 03</span></button>
        <button type="button" data-photo-key="04"><span>Photo 04</span></button>
      </div>
      <button class="secondary-action" type="button" id="galleryButton" data-key="gallery"></button>
      <button class="primary-action" type="button" id="restartButton" data-key="restart"></button>
    </section>`;

  const showToast = message => {
    if (!toast) return;
    toast.textContent = message;
    toast.classList.add("show");
    clearTimeout(state.toastTimer);
    state.toastTimer = setTimeout(() => toast.classList.remove("show"), 2400);
  };

  const addBurst = (x, y) => {
    const words = state.lang === "ru" ? ["УРА", "ВЖУХ", "ЛУНА"] : ["HOWL", "WOW", "MOON"];
    for (let i = 0; i < 5; i += 1) {
      const burst = document.createElement("span");
      burst.className = "burst";
      burst.textContent = words[i % words.length];
      burst.style.left = `${x}px`;
      burst.style.top = `${y}px`;
      burst.style.setProperty("--x", `${Math.random() * 160 - 80}px`);
      burst.style.setProperty("--y", `${Math.random() * -130 - 30}px`);
      document.body.appendChild(burst);
      burst.addEventListener("animationend", () => burst.remove(), { once: true });
    }
  };

  const render = () => {
    screensRoot.innerHTML = screenIntro() + t("scenes").map(screenScene).join("") + screenFinal();
    progress.innerHTML = Array.from({ length: 11 }, () => `<span class="progress-dot"></span>`).join("");
    $("#galleryTitle").textContent = t("galleryTitle");
    $("#secretEyebrow").textContent = t("secretEyebrow");
    $("#secretTitle").textContent = t("secretTitle");
    $("#secretCopy").textContent = t("secretCopy");
    modalCaption.textContent = t("modalCaption");
    document.documentElement.lang = state.lang === "ru" ? "ru" : "en";
    document.querySelectorAll("[data-key]").forEach(element => {
      element.textContent = t(element.dataset.key);
    });
    bind();
    updateScreen();
  };

  const updateScreen = () => {
    document.querySelectorAll(".screen").forEach(screen => {
      screen.classList.toggle("active", Number(screen.dataset.screen) === state.screen);
    });
    document.querySelectorAll(".progress-dot").forEach((dot, index) => {
      dot.classList.toggle("active", index <= state.screen);
    });
  };

  const goTo = screen => {
    state.screen = Math.max(0, Math.min(10, screen));
    updateScreen();
  };

  const loadOptionalImage = target => {
    const key = target.dataset.photoKey;
    const image = new Image();
    image.onload = () => {
      target.textContent = "";
      target.appendChild(image);
    };
    image.onerror = () => {};
    image.alt = `Polina birthday photo ${key}`;
    image.src = photoPath(key);
  };

  const openPhoto = key => {
    modalPreview.textContent = "";
    const image = new Image();
    image.onload = () => {
      modalPreview.textContent = "";
      modalPreview.appendChild(image);
      modalCaption.textContent = photoPath(key);
    };
    image.onerror = () => {
      modalPreview.textContent = t("photoMissing");
      modalCaption.textContent = t("modalCaption");
    };
    image.alt = `Polina birthday photo ${key}`;
    image.src = photoPath(key);
    photoModal.showModal?.();
  };

  const buildGallery = () => {
    galleryGrid.innerHTML = "";
    for (let i = 1; i <= 11; i += 1) {
      const key = String(i).padStart(2, "0");
      const button = document.createElement("button");
      button.type = "button";
      button.dataset.photoKey = key;
      button.innerHTML = `<span>Photo ${key}</span>`;
      loadOptionalImage(button);
      button.addEventListener("click", () => openPhoto(key));
      galleryGrid.appendChild(button);
    }
  };

  const bind = () => {
    document.querySelectorAll("[data-photo-key]").forEach(element => {
      loadOptionalImage(element);
      element.addEventListener("click", () => openPhoto(element.dataset.photoKey));
    });

    document.querySelectorAll("[data-next]").forEach(button => {
      button.addEventListener("click", event => {
        addBurst(event.clientX, event.clientY);
        goTo(state.screen + 1);
      });
    });

    document.querySelectorAll("[data-no]").forEach(button => {
      button.addEventListener("pointerenter", () => {
        state.noCount += 1;
        button.style.setProperty("--no-x", `${Math.round(Math.random() * 90 - 45)}px`);
        button.style.setProperty("--no-y", `${Math.round(Math.random() * 38 - 19)}px`);
        button.style.setProperty("--no-tilt", `${Math.round(Math.random() * 18 - 9)}deg`);
        button.classList.add("is-playful");
        button.textContent = t("noTexts")[state.noCount % t("noTexts").length];
      });
      button.addEventListener("click", event => {
        state.noCount += 1;
        button.classList.add("is-glow");
        button.textContent = t("noTexts")[state.noCount % t("noTexts").length];
        addBurst(event.clientX, event.clientY);
        setTimeout(() => button.classList.remove("is-glow"), 580);
      });
    });

    document.querySelectorAll(".reason-token").forEach(button => {
      button.addEventListener("click", event => {
        const index = Number(button.dataset.reason);
        button.classList.add("is-found");
        state.found.add(index);
        $("#reasonText").textContent = t("reasons")[index];
        addBurst(event.clientX, event.clientY);
        if (state.found.size >= 5) {
          showToast(t("secretToast"));
          setTimeout(() => secretModal.showModal?.(), 450);
        }
      });
    });

    $("#galleryButton")?.addEventListener("click", () => {
      buildGallery();
      galleryModal.showModal?.();
    });

    $("#restartButton")?.addEventListener("click", () => {
      state.found.clear();
      goTo(0);
      render();
    });
  };

  document.querySelectorAll(".lang-option").forEach(button => {
    button.addEventListener("click", () => {
      state.lang = button.dataset.lang;
      document.querySelectorAll(".lang-option").forEach(option => option.classList.toggle("active", option === button));
      render();
    });
  });

  $("#modalClose").addEventListener("click", () => photoModal.close());
  $("#galleryClose").addEventListener("click", () => galleryModal.close());
  $("#secretClose").addEventListener("click", () => secretModal.close());
  [photoModal, galleryModal, secretModal].forEach(dialog => {
    dialog.addEventListener("click", event => {
      if (event.target === dialog) dialog.close();
    });
  });

  musicButton.addEventListener("click", async () => {
    try {
      if (song.paused) {
        await song.play();
        musicButton.classList.add("playing");
        showToast(t("musicPlaying"));
      } else {
        song.pause();
        musicButton.classList.remove("playing");
        showToast(t("musicPaused"));
      }
    } catch (_) {
      showToast(t("musicMissing"));
    }
  });

  render();
})();
